# Level 20 Exercises

1. Draw RLS policies for a `notes` table (private per user).  
2. Explain why `service_role` must never ship in the APK.  
3. Add a `profiles` table and read `display_name` after login.  
4. Compare one feature (todos) implemented in Firebase vs Supabase (paragraph).
